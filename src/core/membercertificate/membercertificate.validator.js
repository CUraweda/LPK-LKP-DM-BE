import Joi from "joi";

export const membercertificateValidator = {
  create: Joi.object({
    // no-data
  }),
  update: Joi.object({
    // no-data
  }),
};

export default membercertificateValidator;
