import Joi from "joi";

export const membertransactionValidator = {
  create: Joi.object({
    // no-data
  }),
  update: Joi.object({
    // no-data
  }),
};

export default membertransactionValidator;
